# def add_5(x: int = 0) -> int:
#     """
#     Add five to any number.

#     :param x: Value to add five to, defaults to ``0``
#     :type x: int
#     :return: Total value
#     :rtype: int
#     """
#     return x + 5


def add_7(x: int = 0) -> int:
    """
    Compute and return the sum of a number and seven.

    Examples:
        >>> add_7(8)
        15
        >>> add(7)
        7

    Args:
        x: A number representing the addend in the addition with seven.

    Returns:
        A number representing the artihmetic sum of `x` and `7`.
    """
    return x + 7
